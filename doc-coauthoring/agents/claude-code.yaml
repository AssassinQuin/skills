interface:
  display_name: "doc-coauthoring"
  short_description: ""
  trigger_words: []
  default_prompt: "Use $doc-coauthoring ..."

# 注：子 Agent 实际用 Claude Code 内置（general-purpose/Explore）或全局 ~/.claude/agents/
# 已注册的（如 brainstorm-collider/oracle/evolver-*）。本 yaml 中原 agents: 块的 role 声明
# 是设计意图文档，Claude Code 运行时不读取，已删除以避免误导。
#
# 原始 agents 块已迁移到 git 历史，如需查阅设计意图：
#   git log --all --oneline -- doc-coauthoring/agents/claude-code.yaml
# 实际调用子 Agent 的方式见各 skill 的 SKILL.md。
