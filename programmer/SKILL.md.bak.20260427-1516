---
name: programmer
description: >
  全流程编程执行引擎。当用户提出开发需求、功能需求、bug修复、功能增强时触发。
  自动执行：项目记忆检索与整理 → 代码考古 → 可选外部调研 → 任务拆解 →
  分批并行实现 → 规范校验 → 记忆更新。
  触发词：「开发」「实现」「新增功能」「加一个」「修复」「改一下」「refactor」
  「feature」「fix」「需求」「帮我做」「add feature」「implement」「build」。
  不触发：纯问答、解释、分析、调研等非编码类请求。
---

# Programmer — 全流程编程执行引擎

## 概览

收到编码需求后，执行 4-Phase 流程。每个 Phase 结束时有明确的产出和检查点。

```
Phase 0 (条件): 首次使用 → 记忆整理
Phase 1 (并行): 记忆检索(含规范合并) + 代码考古 + [可选调研] → 产出: docs/dev-*/overview.md
   ↓ 简单需求可跳过 Phase 2，直接进入 Phase 3
Phase 2 (串行): 任务分析 + 规划 → 产出: docs/dev-*/task-plan.md
   ↓ 用户确认执行计划后进入 Phase 3
Phase 3 (并行): 智能调度(≤3子进程,精确分配agent) + 子任务同步维护 → 产出: 代码变更 + 更新的 task-plan.md
   ↓
Phase 4 (串行): 质量洞察收集 + 收尾 + 记忆更新 + 清理
```

**语言要求：所有面向用户的提问、确认、展示，一律使用中文。**

---

## Phase 0: 首次使用记忆整理

### 触发条件

当 Phase 1.1 的记忆检索返回结果**混乱、重复、或无项目 tag**时，说明该项目记忆未经整理。

判断标准：
- 同一信息存在 ≥2 条记忆
- 记忆缺少项目 tag，无法区分归属
- 记忆内容相互矛盾

### 执行流程

```
1. memory_list(tags=["{项目目录名}"]) → 获取全部项目记忆
2. 分类审查:
   - 重复内容 → 合并为一条精炼记忆，删除多余的
   - 矛盾内容 → 向用户确认哪个正确
   - 缺少 tag → 补充 "{项目目录名}" tag
   - 过时内容 → 删除
3. 生成规范摘要: 将整理后的记忆合并为一份「项目规范摘要」
   content: "{project} 项目规范摘要:\n{所有有效规范的精炼列表}"
   tags: ["{项目目录名}", "规范摘要"]
   type: "convention"
4. 向用户展示整理结果并确认
```

展示格式（中文）：
```
📋 首次使用，整理项目记忆:
🔄 [合并] 3条重复的技术栈记录 → 合并为1条
🗑️ [删除] 过时: 旧版依赖列表
✏️ [补充] 缺少标签: {具体内容}
📌 [生成] 项目规范摘要

是否确认以上整理？
```

**注意**: Phase 0 仅在首次使用或记忆明显混乱时触发。后续使用跳过。

---

## Bug 修复专项规范

### 核心原则

Bug 修复**不得敷衍**。每一次修复都应视为改善代码健康度的机会。

### 修复等级判定

| Bug 特征 | 等级 | 要求 |
|---------|------|------|
| 单点错误，根因明确 | 快速修复 | 修复 + 补测试 + 踩坑记录 |
| 多处相似错误，同一根因 | 系统修复 | 统一修复所有实例 + 补测试 + 记录 |
| 修复需改动核心逻辑/架构 | **重构级修复** | 按 Phase 2 完整流程拆解，评估重构必要性 |
| 修复引入新风险 | 防御修复 | 必须有回归测试 + 边界测试 |

### 修复执行要求

```
1. 根因分析（必须）:
   - 不允许只修表面症状
   - 必须追溯 bug 的根本原因
   - 在 task-plan.md 中记录: 现象 → 根因 → 修复方案

2. 方案评估:
   - 是否只是打补丁？能否通过重构从根本上消除此类 bug？
   - 修复是否影响其他模块？影响范围评估
   - 是否存在同类 bug 在其他位置？（全局搜索同类模式）

3. 可维护性考虑:
   - 修复后的代码是否比原来更容易理解？
   - 是否需要添加防御性代码（参数校验、错误处理）？
   - 是否需要添加/更新注释和文档？
   - 修复是否为后续扩展预留了空间？

4. 禁止行为:
   ❌ 使用特殊条件硬编码绕过问题（如 if (特殊值) return）
   ❌ 只修报告的个案，忽略同类问题
   ❌ 用 try-catch 吞掉异常而不处理
   ❌ 修复后不补测试
   ❌ 不评估重构必要性的快速 hack

5. 重构评估触发条件（满足任一即触发）:
   - 同一 bug 反复出现（≥2次）
   - 修复需要修改 ≥3 个文件
   - 代码中存在明显的设计不合理（如上帝类、循环依赖）
   - 修复后代码复杂度反而增加
```

### 修复完成标准

- [ ] 根因已识别并记录
- [ ] 同类问题全局排查完毕
- [ ] 新增或更新了回归测试
- [ ] 修复方案不会增加技术债务
- [ ] 踩坑记录已存储到项目记忆

---

## 上下文压缩规范

当使用 `compress` 工具进行上下文压缩时，**必须保留以下关键信息**，确保压缩后的信息完整性不失效：

### 必须保留的信息清单

```
1. 项目规范摘要（最高优先级）
   - 项目级规范（带 [项目] 标签的全部条目）
   - 通用规范基线（带 [通用] 标签的关键条目）
   - 冲突覆盖记录（带 [项目，覆盖通用] 的条目）
   → 压缩时原样保留规范表格，不做精简

2. 全局标准
   - 通用编码规范
   - 技术栈约束
   - 文件大小限制规则
   → 保留完整列表

3. 项目进展状态
   - 当前 task-plan.md 中所有任务的状态（⬜⬜🔄✅❌）
   - 已完成任务的产出文件路径
   - 进行中任务的当前批次和进度
   - 失败任务的原因记录
   → 保留任务状态表格

4. 开发文档位置
   - docs/dev-{ts}-{name}/ 目录路径
   - overview.md、task-plan.md、research.md 的关键结论
   → 保留路径和摘要

5. 架构决策与设计意图
   - Phase 2 中 oracle 的分析结论
   - 关键的「为什么这样做」的决策理由
   → 压缩为精炼的决策列表

6. 质量洞察发现
   - 已发现的代码重复、设计问题等
   - 给出的改进建议
   → 保留发现列表
```

### 压缩模板

```markdown
## 项目规范（不可精简）
{规范表格，含来源标注}

## 项目进展
- 任务状态: T1✅ T2🔄(batch2/3) T3⬜
- 文档目录: docs/dev-20260427-1430-xxx/
- 关键决策: {决策列表}
- 失败记录: {若有}

## 质量洞察
{已发现的问题和建议}

## 当前上下文
{当前正在执行的任务详情}
```

### 压缩禁忌

- ❌ 不得精简规范表格（必须保留每条规范和来源标注）
- ❌ 不得丢失任务状态信息（所有任务的状态必须保留）
- ❌ 不得省略失败原因（失败任务的根因分析必须保留）
- ❌ 不得丢弃架构决策理由（"为什么这样做"比"做了什么"更重要）

---

## Agent 职责矩阵（全流程）

本 skill 涉及 **5 大类任务**，根据 agent 特性精确分配，贯穿 Phase 1-4：

### 按任务类型分配

| 任务类型 | 负责 Agent | 原因 | 触发阶段 |
|---------|-----------|------|---------|
| **代码搜索/考古** | **@explorer** | 2x 搜索速度，1/2 成本，擅长 glob/grep/AST 定位 | Phase 1.2 |
| **网页内容搜索/外部调研** | **@librarian** | 10x 查找最新文档能力，擅长 API 参考、版本差异 | Phase 1.3, Phase 3 按需 |
| **代码处理/实现** | **@fixer** | 2x 执行速度，1/2 成本，擅长读写的确定性实现 | Phase 3 全部实现任务 |
| **分析/评审/重构** | **@oracle** | 5x 决策能力，擅长架构权衡、复杂调试、代码评审 | Phase 2.1, Phase 3 重构类任务 |
| **计划文档生成** | **主进程自身** | 轻量文档生成无需子进程，主进程直接写入更可靠 | Phase 1.4, Phase 2.2, Phase 4.1 |
| **UI/样式处理** | **@designer** | 10x UI/UX 质量，擅长组件架构、视觉一致性 | Phase 3 UI 类任务 |
| **记忆检索/存储** | **主进程自身** | memory 工具调用轻量，无需子进程 | Phase 1.1, Phase 4.2-4.3 |

### 按阶段调度全景

```
Phase 1:
  并行组 A: 主进程(记忆检索) + @explorer(代码考古) + @librarian?(外部调研)
  → 3 进程上限内，@librarian 仅条件触发

Phase 2:
  @oracle(任务分析与拆解) — 串行，需要完整上下文
  主进程(生成 task-plan.md + 用户确认)

Phase 3:
  调度循环（每轮 ≤3 子进程）:
  - @fixer × N  → 实现类任务
  - @oracle × 1 → 重构评审任务（串行）
  - @designer × N → UI/样式任务
  - @librarian? → 按需查新 API（与 @fixer 并行）
  主进程(文档更新、子任务同步、调度决策)

Phase 4:
  主进程(质量洞察汇总 + 记忆更新 + 清理) — 无需子进程
```

### 调度约束

1. **同一时刻 ≤3 个子进程**（所有类型合计）
2. **同类型 agent ≤2 个并行**（避免资源争抢）
3. **@oracle 评审任务串行**（需要完整上下文）
4. **@librarian 可与 @fixer 并行**（调研不阻塞实现）
5. **有依赖关系的任务由同一 agent 处理**（保证上下文连续性）
6. **主进程负责调度决策和文档写入**（不启动子进程做轻量工作）

---

## Phase 1: 信息收集

### 启动条件

收到任何编码需求后立即进入。按「Agent 职责矩阵」分配子任务：

### 1.1 项目记忆检索

并行发 3+1 个 `memory_search` 查询：

```
query: "项目规范 开发规范 编码规范", tags: ["{项目目录名}"]
query: "注意事项 约定 踩坑",        tags: ["{项目目录名}"]
query: "技术栈 架构 设计决策",       tags: ["{项目目录名}"]
query: "通用规范",                   tags: ["通用规范"]
```

**规范合并与冲突解决策略**：

汇总为「项目规范摘要」，合并规则：
1. 以项目级规范为主体
2. 全局通用规范作为基线补充（项目未定义的领域由通用规范兜底）
3. **冲突时项目规范优先** — 当项目规范与通用规范冲突，以项目规范为准
4. 在 overview.md 中明确标注每条规范的来源：`[项目]` 或 `[通用]` 或 `[项目，覆盖通用]`

示例：
```
- 命名约定: camelCase [项目]
- 单文件 ≤ 800 行 [通用]
- 使用函数式组件 [项目，覆盖通用规范"使用类组件"]
```

若返回结果混乱（重复、矛盾），触发 Phase 0。

**新项目处理**: 若 3 个查询均无结果，标记「新项目」。Phase 4 自动存储初始技术栈和约定。

### 1.2 代码考古 — @explorer

启动 **@explorer**，传入以下指令：

```
项目根目录: {project_root}
需求: {user_requirement}

请完成:
1. 浏览项目顶层目录结构，识别技术栈和项目类型
2. 根据需求关键词，用 glob + grep 定位相关文件
3. 读取关键文件，记录: 文件路径 | 关键函数/类名 | 行号范围 | 依赖关系
4. 识别代码组织模式和命名约定

输出格式:
## 项目技术栈
## 相关文件清单（表格）
## 项目约定（目录结构、命名、代码风格）
```

### 1.3 外部调研 — 条件执行

**仅当**以下条件之一成立时启动 **@librarian**：
- 需求涉及项目未使用的框架或库
- 需要集成新的外部服务/API
- 技术选型有 ≥2 种合理方案

若需求涉及的文件/技术栈在 1.2 中已充分了解，**跳过**。

启动 @librarian 时，传入技术栈 + 需求，要求输出：方案对比（≥2种）、优缺点、项目适配度评估。
输出格式参见 [references/research-template.md](references/research-template.md)。

### 1.4 生成开发文档

等 1.1 + 1.2 完成后（1.3 可能跳过），在 `docs/` 下创建：

```
docs/dev-{YYYYMMDD-HHmm}-{需求简述}/
├── overview.md      ← 1.1 规范摘要 + 1.2 考古结果
├── research.md      ← 1.3 调研报告（若有）
└── task-plan.md     ← Phase 2 生成
```

**overview.md** 开头必须包含：
```markdown
> **本文件功能**: 记录 {需求} 的项目架构分析、相关文件清单和开发约束。
```

### 1.5 复杂度判定与分流

完成 Phase 1 后，根据下表判定路径：

| 条件 | 级别 | 路径 |
|------|------|------|
| 单文件改动 < 50 行，无新依赖 | 简单 | → 直接 Phase 3，跳过 Phase 2 |
| 涉及 2-3 个文件 | 中等 | → Phase 2 拆 2-3 个子任务 |
| 涉及 3+ 文件或新模块/新依赖 | 复杂 | → Phase 2 完整拆分 + 必须有调研 |

**简单需求直接进入 Phase 3，用 overview.md 中的信息指导实现。**

---

## Phase 2: 任务分析与规划

### 2.1 @oracle 分析

启动 **@oracle**，指令：

```
项目根目录: {project_root}
需求: {user_requirement}
开发文档目录: {dev_doc_dir}

请阅读:
1. {dev_doc_dir}/overview.md
2. {dev_doc_dir}/research.md（若有）
3. 项目规范摘要（附在下面）

任务: 将需求拆解为子任务，每个子任务包含:
- 任务编号 (T1, T2, ...)
- 任务标题
- 涉及文件和行号
- 具体改动描述
- 依赖的前置任务编号
- 预估复杂度 (低/中/高)
- 标注哪些任务可并行

拆解要求:
- 单个文件预估超过 500 行 → 拆为「框架生成」+「内容分批填充」子任务
- 单个文件预估超过 1000 行 → 必须拆分为多个文件/模块，向用户说明拆分方案

输出: 写入 {dev_doc_dir}/task-plan.md
```

task-plan.md 模板参见 [references/task-plan-template.md](references/task-plan-template.md)。

### 2.2 用户确认检查点 🔒

**生成 task-plan.md 后，暂停并用中文展示给用户**：

```
📋 执行计划已生成:
- T1: {标题} (复杂度: 低, 无依赖)
- T2: {标题} (复杂度: 中, 依赖 T1)
- T3: {标题} (复杂度: 低, 依赖 T1)

并行组: [T2, T3] 可在 T1 完成后并行执行

是否开始执行？可以调整任务范围或顺序。
```

**用户确认后才进入 Phase 3。** 若用户要求修改，更新 task-plan.md 后重新确认。

---

## Phase 3: 并行实现

### 3.1 大文件生成策略

**当某个任务需要生成 500+ 行的文件**，且记忆中没有该文件的明确生成规范时，必须按以下流程执行：

```
步骤 1: 框架生成
  - 生成文件骨架：类/函数签名、import、模块结构、占位注释
  - 产出: 可运行的空壳文件，所有函数有 docstring 和 pass/TODO

步骤 2: 分批填充内容
  - 按功能模块分批（每批 ≤ 300 行）
  - 每批独立作为一个子任务
  - 标注: "T{n}-batch1: {模块名} 实现"

步骤 3: 逐批验证
  - 每批完成后检查:
    ✅ 逻辑是否与需求一致
    ✅ 与前序批次是否衔接正确
    ✅ 是否引入冲突或重复
  - 验证失败 → 修正后继续，不跳过

步骤 4: 整体验证
  - 所有批次完成后:
    ✅ 完整逻辑链路验证
    ✅ 总行数是否控制在合理范围
    ✅ 函数/模块职责是否清晰无重叠
```

**当文件预估超过 1000 行**：
- 优先拆分为多个文件/模块（每个 ≤ 800 行）
- 确实无法拆分时，向用户说明原因并获取例外许可
- 立即将此拆分规范存储到项目记忆中，避免后续重复

### 3.2 调度策略

根据「Agent 职责矩阵」和 task-plan.md 的依赖图，执行调度循环：

**子进程限制**: 同一时刻最多启动 **3 个**子进程（含所有类型 agent）。

**Phase 3 任务路由表**（实现阶段细分）：

| 任务特征 | 推荐 Agent | 协作模式 |
|---------|-----------|---------|
| 纯实现类（新增功能、填充代码） | **@fixer** | 独立执行 |
| 测试编写/更新 | **@fixer** | 独立执行，可与实现任务并行 |
| 需要架构/重构判断的修改 | **@oracle** → 评审 → **@fixer** 实现 | 先评审出方案，再交 fixer 执行 |
| 涉及 UI/样式/交互的修改 | **@designer** | 独立执行 |
| 实现中需要查新 API/文档 | **@librarian** 查询 → 结果传给 **@fixer** | 先查后做，或并行查询+准备 |
| Bug 修复（简单） | **@fixer** | 独立执行，遵守 Bug 修复专项规范 |
| Bug 修复（重构级） | **@oracle** 评审方案 → **@fixer** 实现 | 遵守 Bug 修复专项规范的重构级要求 |

**调度循环**：
```
while (存在未完成任务):
  1. 从 task-plan.md 获取所有「就绪任务」
  2. 按路由表为每个任务匹配 agent 类型
  3. 根据调度约束（≤3进程, 同类≤2, oracle串行）选择本轮启动的子集
  4. 启动子进程，传入对应 agent 指令模板
  5. 等待任一子进程完成 → 更新 task-plan.md → 检查同步点
  6. 检查是否有新任务变为「就绪」→ 继续循环
```

**调度示例**：
```
就绪任务: T2(实现 API), T3(写测试), T4(前端组件)
→ @fixer→T2, @fixer→T3, @designer→T4 (3个并行)

就绪任务: T5(重构核心模块), T6(修复样式)
→ @oracle→T5(先评审方案), @designer→T6 (2个并行，T5评审后@fixer执行)

就绪任务: T7(实现新API集成), T8(继续前序任务)
→ @librarian→T7(查API文档), @fixer→T8 (2个并行，T7查询完成后@fixer实现)
```

### 3.3 Agent 指令模板

根据 3.2 路由结果，为不同 agent 传入对应指令：

**@fixer 指令**（纯实现/测试任务）：

```
项目根目录: {project_root}
开发文档目录: {dev_doc_dir}

请阅读:
1. {dev_doc_dir}/overview.md
2. {dev_doc_dir}/task-plan.md 中 T{n} 的内容

任务: 执行 T{n}

严格遵循:
- 项目规范（见 overview.md 规范章节）
- 不修改 T{n} 范围外的文件
- 保持现有代码风格
- 文件行数控制: 单文件不超过 800 行，超出必须拆分模块
- 若任务属于大文件分批填充，先阅读已完成的批次，确保衔接
- 完成后更新 task-plan.md 中 T{n} 状态为 ✅ 已完成
  并添加完成说明
- 若在实现中发现任何异常（代码重复、设计问题、性能隐患），
  在完成说明中附加「发现」段落
```

**@oracle 指令**（架构/重构评审任务）：

```
项目根目录: {project_root}
开发文档目录: {dev_doc_dir}

请阅读:
1. {dev_doc_dir}/overview.md
2. {dev_doc_dir}/task-plan.md 中 T{n} 的内容
3. 相关源文件（按 task-plan 中标注的路径）

任务: 评审 T{n} 的实施方案

输出:
1. 推荐的实现方案（含理由）
2. 需要注意的风险点
3. 是否建议重构（若需要，给出重构方向）
4. 实现步骤拆解（供 @fixer 执行）

将评审结论写入 {dev_doc_dir}/task-plan.md T{n} 的评审备注中
```

**@designer 指令**（UI/样式任务）：

```
项目根目录: {project_root}
开发文档目录: {dev_doc_dir}

请阅读:
1. {dev_doc_dir}/overview.md
2. {dev_doc_dir}/task-plan.md 中 T{n} 的内容

任务: 执行 T{n}（UI/样式相关）

严格遵循:
- 项目设计系统和组件规范
- 响应式布局要求
- 保持与现有 UI 风格一致
- 完成后更新 task-plan.md 中 T{n} 状态
```

### 3.4 执行规范

所有子 agent **必须**遵守：
1. 严格遵守 overview.md 中的项目规范（含通用规范基线）
2. 不得修改任务范围外的文件
3. 代码风格与项目现有代码一致
4. **单文件不超过 800 行** — 超出时拆分为多个模块
5. 每个任务完成后**立即**更新 task-plan.md
6. 若发现需额外改动，**先报告**，不擅自修改范围外文件
7. **发现即报告** — 实现过程中发现的任何异常（重复代码、设计问题、性能隐患），必须在 task-plan.md 中标注

### 3.5 异常处理

| 异常情况 | 处理方式 |
|---------|---------|
| 子 agent 执行失败 | 标记该任务为 ❌ 失败，记录原因，继续其他任务 |
| 子 agent 发现需求理解偏差 | 暂停执行，用中文向用户确认后再继续 |
| 任务间冲突（同一文件被多个任务修改） | 串行化这些任务，按 task-plan 顺序执行 |
| 环境问题（缺少依赖等） | 记录到 task-plan.md，尝试自动修复，失败则报告用户 |
| 文件超过 800 行 | 自动拆分为多个模块文件，更新 task-plan.md |
| 分批填充验证失败 | 回退当前批次，修正后重新生成，向用户报告 |

### 3.6 子任务同步维护机制

**当执行过程中发生以下情况时，必须同步更新 task-plan.md**：

| 变更事件 | 同步操作 |
|---------|---------|
| 新增子任务（用户追加、修复中发现需额外改动） | 在 task-plan.md 中追加任务编号 T{n+1}，标注来源和依赖 |
| 删除/取消子任务 | 标记为 🚫 已取消，记录取消原因 |
| 修改子任务范围 | 更新任务描述，标注变更时间和原因 |
| 子任务拆分 | 将原任务改为父任务，拆出子任务 T{n}.1, T{n}.2... |
| 任务依赖变更 | 更新依赖图，重新评估并行组 |
| 优先级调整 | 更新优先级标注，调整执行顺序 |

**同步规则**：

```
1. 唯一真相源: task-plan.md 是任务状态的唯一真相源
   - 任何子 agent 的任务变更都必须反映到 task-plan.md
   - 子 agent 不允许私下变更，必须通过主进程协调

2. 变更追溯: 每次变更记录格式:
   > [{时间}] {变更类型}: T{n} {变更内容} (原因: {why})

3. 分支管理: 若用户在中途提出新需求（非当前任务范围）
   - 在 task-plan.md 末尾新增「待处理」章节
   - 记录新需求，标注「来源: 中途追加」
   - 当前任务完成后统一处理，不中断进行中的任务

4. 跨任务一致性:
   - 若 T{n} 的实现影响了 T{m} 的前提假设
   - 立即检查 T{m} 的任务描述是否需要更新
   - 更新后重新评估 T{m} 的复杂度和依赖关系

5. 定期同步点:
   - 每个子 agent 完成时 → 同步
   - 每轮调度循环开始时 → 检查 pending 变更
   - Phase 3 结束时 → 全量一致性校验
```

---

## Phase 4: 收尾与记忆更新

### 4.0 质量洞察收集与报告

在进入收尾前，汇总全流程中发现的所有质量问题，形成洞察报告。

**洞察触发场景**：

| 场景 | 洞察类型 | 检测方式 | 建议行动 |
|------|---------|---------|---------|
| 测试运行时间 >30s | 性能隐患 | 观察测试执行耗时 | 建议拆分测试套件、使用 mock、并行测试 |
| 同一逻辑在 ≥3 处出现 | 代码重复 | AST 搜索相似模式 | 建议抽取公共函数/工具类 |
| 函数参数 >5 个 或函数体 >50 行 | 设计不合理 | 代码审查 | 建议拆分函数、引入参数对象 |
| 深层嵌套 ≥4 层 | 可读性差 | AST 分析 | 建议提取函数、使用卫语句 |
| 文件间循环依赖 | 架构问题 | import 分析 | 建议引入中间层/事件机制 |
| 硬编码的配置值 | 可维护性差 | grep 常见硬编码模式 | 建议抽取配置文件/环境变量 |
| 缺少错误处理的 API 调用 | 健壮性隐患 | 代码审查 | 建议添加错误处理和重试机制 |
| 数据库查询未做分页/限制 | 性能隐患 | SQL/API 审查 | 建议添加分页、限制返回数量 |

**执行方式**：

```
1. 汇总子 agent 在 task-plan.md 中标记的「发现」段落
2. 在代码考古和实现过程中主动检测上表中的场景
3. 生成洞察报告，格式：

📋 质量洞察报告:
⚠️ [性能] tests/test_api.py 运行耗时 45s → 建议: 拆分为单元测试+集成测试
⚠️ [重复] 用户验证逻辑在 3 个文件中出现 → 建议: 抽取 auth_utils.py
💡 [改进] config.py 中 6 个硬编码值 → 建议: 迁移到 .env
💡 [架构] module_a ↔ module_b 循环依赖 → 建议: 引入 event_bus 中间层

4. 将洞察存入项目记忆:
   content: "{project} 质量洞察: {类型} - {描述} → 建议: {建议}"
   tags: ["{项目目录名}", "质量洞察"]
   type: "note"

5. 在 Phase 4.4 用户确认时一并展示
```

**洞察原则**：
- 只报告有价值的发现，不制造噪音
- 每条洞察必须附带可操作的建议
- 不阻塞当前开发流程，作为附加产出

### 4.1 开发文档收尾

- task-plan.md: 确认所有任务状态（✅ 或 ❌）
- overview.md: 若有架构变更，更新文件清单
- 如有失败任务，记录原因和后续建议

### 4.2 记忆更新

按以下类型**逐条**存储（每条独立调用 `memory_store`）：

| 类型 | content 模板 | tags | type |
|------|-------------|------|------|
| 技术决策 | `"在 {project} 中，{决策}，原因: {原因}"` | `["{项目目录名}", "技术决策"]` | `"decision"` |
| 开发规范 | `"{project} 规范: {规范内容}"` | `["{项目目录名}", "开发规范"]` | `"convention"` |
| 踩坑记录 | `"{project} 踩坑: {问题} → 解决: {方案}"` | `["{项目目录名}", "踩坑"]` | `"bug"` |
| 新项目初始化 | `"{project} 技术栈: {栈}; 命名: {约定}"` | `["{项目目录名}", "技术栈"]` | `"fact"` |
| 大文件拆分规范 | `"{project} 大文件拆分: {模块} → {拆分方案}"` | `["{项目目录名}", "架构", "拆分"]` | `"decision"` |

用户明确提出的通用规范（不限于当前项目），额外存储：
```
content: "通用规范: {规范内容}"
tags: ["通用规范"]
type: "convention"
``

### 4.3 记忆清理与优化

```
1. memory_list(tags=["{项目目录名}"]) → 获取当前项目全部记忆
2. 逐条审查:
   过时/已解决 → memory_delete
   重复 → 合并为一条，删除旧的
   措辞不精确 → memory_update 优化
3. 用中文展示清单给用户确认
```

展示格式（中文）：
```
📋 {project} 记忆清单:
✅ [保留] 技术栈: Python 3.12 + FastAPI
🗑️ [删除] 过时: 旧版依赖列表
✏️ [优化] 踩坑: → 重写为更准确的描述
```

### 4.4 用户确认检查点 🔒

**Phase 4 完成后，用中文展示执行摘要**：

```
✅ 开发完成
- 任务: T1✅ T2✅ T3✅
- 文档: docs/dev-{ts}-{name}/
- 记忆更新: {N} 条新增，{M} 条清理
- 子任务变更: {X} 条新增/修改/取消

📋 质量洞察:
⚠️ {洞察1}
💡 {洞察2}
(详见 task-plan.md 质量洞察章节)
```

---

## 通用规则

1. **中文交互** — 所有面向用户的提问、确认、状态展示一律使用中文
2. **文档概述先行** — 每个生成文档开头必须有 `> **本文件功能**:` 概述
3. **规范严格执行** — Phase 1.1 检索到的规范，全流程遵守
4. **规范冲突解决** — 项目规范与通用规范冲突时，项目规范优先
5. **范围严格控制** — 不允许修改任务范围外文件
6. **状态实时更新** — task-plan.md 每步更新
7. **记忆按项目隔离** — 所有记忆 tags 包含项目目录名
8. **通用规范单独存储** — 跨项目规范用 `"通用规范"` tag
9. **异常不静默** — 任何失败必须用中文报告用户
10. **新项目自动初始化** — 首次开发后自动存储项目技术栈和约定
11. **单文件 ≤ 800 行** — 超出必须拆分模块，1000+ 行为硬上限
12. **大文件分批生成** — 500+ 行文件按「框架→分批填充→逐批验证」流程执行
13. **首次使用整理记忆** — 记忆混乱时自动触发 Phase 0 整理
14. **Bug 修复不敷衍** — 必须追根因、查同类、补测试、评重构
15. **上下文压缩保完整** — 压缩时必须保留规范、进展、决策等关键信息
16. **子进程 ≤3 且精确分配** — 5 类任务按 Agent 职责矩阵分配：代码搜索→@explorer，网页搜索→@librarian，代码处理→@fixer，分析重构→@oracle，计划文档→主进程自身
17. **发现即报告** — 实现中发现的问题（重复代码、设计问题、性能隐患）必须标注
18. **子任务同步维护** — 任何变更实时同步到 task-plan.md，保持唯一真相源
